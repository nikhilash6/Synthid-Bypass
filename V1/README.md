# V1 Archive

This folder contains the original multi-workflow release that shipped before the consolidated v2 workflow.

Contents:

- `workflows/` for the old v1 workflow JSON files
- `comparison/` for the archived before/after examples
- `check/` for the archived SynthID detector screenshots
- `workflow_screenshots/` for the legacy graph screenshots

Nothing in this folder is the current recommended path. The active release now lives at the repository root.
